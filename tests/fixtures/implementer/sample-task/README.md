# sample-task fixture

Used by `src/agents/implementer.test.ts` as a trivial worktree the
implementer agent operates within. Tests copy this directory to a tmpdir
before running so they never mutate the fixture itself.
